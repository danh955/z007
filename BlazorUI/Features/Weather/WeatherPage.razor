@page "/weather"

<h1>Weather forecast</h1>

<p>This component demonstrates fetching data from a service.</p>

@if (model == null)
{
    <p><em>Loading...</em></p>
}
else
{
    <table class="table-auto">
        <thead>
            <tr>
                <th>Date</th>
                <th>Temp. (C)</th>
                <th>Temp. (F)</th>
                <th>Summary</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var forecast in model.Forecasts)
            {
                <tr>
                    <td>@forecast.Date.ToShortDateString()</td>
                    <td class="text-right">@forecast.TemperatureC</td>
                    <td class="text-right">@forecast.TemperatureF</td>
                    <td>@forecast.Summary</td>
                </tr>
            }
        </tbody>
    </table>
}